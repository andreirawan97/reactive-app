import avatars from './avatars';

type ItemTypes = 'avatars';

const shopItems = {
  avatars: [
    avatars.fallenCoder,
    avatars.stickman,
    avatars.millionaire,
    avatars.semicolon,
    avatars.theAtom,
  ],
};

export default shopItems;
