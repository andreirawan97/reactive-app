import { NoAvatar } from '../../../assets';

import { Avatar } from '.';

const noAvatar: Avatar = {
  id: 'noAvatar',
  name: 'No Avatar',
  source: NoAvatar,
  price: 0,
};

export default noAvatar;
